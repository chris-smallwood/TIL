

###test-db.ts in programtracker
- Using a node package that is designed to be used with multiple runtimes e.g. node, bun, deno. etc.
- This is why we need to pass node built in modules as args so the function can also take other runtime modules as args.
